<div id="<%= dom_id report %>">
  <p>
    <strong><%= Report.human_attribute_name(:title) %>:</strong>
    <%= report.title %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:text) %>:</strong>
    <%= report.text %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:created_date) %>:</strong>
    <%= l report.created_at, format: :long %>
  </p>

</div>
